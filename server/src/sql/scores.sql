-- ==========================================
-- 乐谱模块 SQL 语句
-- ==========================================

-- 查询所有乐谱
-- SELECT * FROM scores ORDER BY updated_at DESC;

-- 根据 ID 查询乐谱
-- SELECT * FROM scores WHERE id = ?;

-- 搜索乐谱（按名称或作曲者）
-- SELECT * FROM scores WHERE name LIKE ? OR composer LIKE ? ORDER BY updated_at DESC;
